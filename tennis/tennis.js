let home = 0;
let away = 0;
let homeSet = 0;
let awaySet = 0;
let advantage = '';


//WORK IN PROGRESS
/*
function increaseScore(team) {
  if (team < 30) {
    team += 15;
    drawScores();
  } else if (team == 30) {
    team += 10;
    drawScores();
  } 
  if (home == 40 && away < 40) {
    advantage = 'home';
  } else if (home < 40 && away == 40){
    advantage = 'away';
  } else if (home == 40 && away == 40){
    advantage = 'deuce';
  } else {
    advantage = '';
  }
};

function decreaseScore(team) {
  if (team >= 40) {
    team -= 10;
  } else if (team <= 30 && team > 0){
    team -= 15;
  }
  if (team == home && advantage == 'home')
}

  function drawScores() {
    let homeScore = document.getElementById('home-score');
    let awayScore = document.getElementById('away-score');
    homeScore.innerText = home;
    awayScore.innerText = away;
  }
  */